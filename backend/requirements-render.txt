# ============================================
# AI Chatbot Backend — Production Dependencies (Lightweight)
# ============================================
# Use this file for deployment on Render, Railway, or any
# storage-limited platform. It uses the HF Inference API for
# embeddings instead of local PyTorch (~2.5 GB savings).
#
# Install with: pip install -r requirements-render.txt
#
# REQUIRED env vars on your hosting platform:
#   HUGGINGFACE_API_KEY=hf_your_token_here
#   USE_CLOUD_EMBEDDINGS=true
# ============================================

# ---- Web Framework ----
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# ---- LLM & RAG ----
langchain>=0.3.0
langchain-community>=0.3.0
langchain-groq>=0.3.0

# ---- Embeddings & Vector Store (CLOUD MODE) ----
# No sentence-transformers or PyTorch needed!
# Embeddings are generated via the free HF Inference API.
langchain-huggingface>=0.2.0
huggingface-hub>=0.30.0
faiss-cpu>=1.15.0

# ---- Database ----
sqlalchemy>=2.0.0
aiosqlite>=0.21.0

# ---- Document Processing ----
pypdf>=5.0.0
python-docx>=1.1.0

# ---- Configuration ----
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# ---- Streaming ----
sse-starlette>=2.0.0

# ---- File Upload ----
python-multipart>=0.0.20
